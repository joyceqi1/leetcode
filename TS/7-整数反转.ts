function reverse(x: number): number {
    let str:string = '' + x, prefix:string=x[0]
    if(str.length === 1){
        return x
    }
    if(prefix === '-'){
        
    }
    return parseInt(str.split('').reverse().map((item, index) => {
        if()
    }).join(''))
};
const x:number = 123